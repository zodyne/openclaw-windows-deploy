# 技能准入反臃肿规约(Stage 5 启用前置,2026-07-04 立)

> 对应 v3 方案 §3.4 设计决议。Stage 5(自进化)启用时,进化门禁必须先实现本文件三条准入检查,缺一不开闸。

1. **先查重后生成**:候选技能入门禁前对能力目录语义检索;相似度 ≥0.85 → 强制转为演进现有技能(版本升级/触发扩展),新建须证明不可合并。默认改旧的。
2. **触发冲突零容忍**:触发模式重叠而行为不一致 → 阻断,人工裁决 supersede 关系后才可继续。
3. **活跃上限 30-50**:超限新增必须伴随一次淘汰。

健康度 KPI:「检索命中即正确」率(golden 增设 skill-selection 断言,选错技能=回归),不以数量论健康。
定期体检(合并相似/淘汰低效/沉淀元技能)保留为兜底,入口把关优先。

预登记的一号候选:<your-project> 周报(memory/ucm221-weekly-excel-spec.md 已具规格雏形)——启用时以它做全链路演练:候选→查重→回放→审批→入库。
